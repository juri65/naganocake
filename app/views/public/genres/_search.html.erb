<table class="table table-bordered style="width: 12rem;">
  <tr><th>ジャンル検索</th></tr>
    <% genres.each do |genre| %>
  <tr><td><%= link_to genre.name,genre_path(genre) %></td></tr>
  <% end %>
</table>